# AI-Uncode Landing Page

صفحة هبوط عصرية ومتجاوبة بالكامل لشركة SaaS باسم "AI-Uncode" مبنية باستخدام Next.js 16 و Tailwind CSS مع دعم كامل للغة العربية واتجاه RTL.

## 🏗️ البنية المعمارية

تم بناء هذا المشروع وفقًا لمبادئ معمارية صارمة لضمان سهولة الصيانة والتوسع:

### 1. فصل الاهتمامات والتحكم المركزي

**ملف الإعدادات المركزي:** `src/config/landingPageConfig.ts`

- يحتوي على **جميع** محتوى وبيانات الصفحة
- يدعم متغيرات A/B Testing للعناوين الرئيسية
- يتحكم في إظهار/إخفاء الأقسام عبر مؤشرات boolean
- المكونات stateless تقرأ فقط من هذا الملف

**مثال على تغيير المحتوى:**

\`\`\`typescript
// في src/config/landingPageConfig.ts
export const config = {
  hero: {
    title: {
      variantA: 'عنوان جديد هنا',
      active: 'variantA' // غيّر إلى variantB أو variantC لاختبار A/B
    }
  }
}
\`\`\`

**مثال على إخفاء قسم:**

\`\`\`typescript
sections: {
  hero: true,
  painPoints: false, // سيتم إخفاء هذا القسم
  solution: true,
}
\`\`\`

### 2. الهوية البصرية المركزية

**ملف الألوان:** `app/globals.css`

جميع ألوان الهوية البصرية معرّفة في `:root`:

\`\`\`css
:root {
  --color-primary: #007BFF;    /* أزرق - الثقة والديناميكية */
  --color-secondary: #28A745;  /* أخضر - النمو والإيجابية */
  --color-accent: #FFC107;     /* أصفر ذهبي - الإبداع */
  --color-background: #F8F9FA; /* رمادي فاتح - الراحة البصرية */
  --color-text-main: #212529;  /* رمادي داكن - الوضوح */
  --color-text-light: #6C757D; /* رمادي متوسط - التباين */
}
\`\`\`

**استخدام الألوان في المكونات:**

\`\`\`tsx
// ✅ صحيح - استخدام الألوان المخصصة
<div className="bg-[var(--color-primary)] text-white">

// ❌ خطأ - تجنب الألوان المباشرة
<div className="bg-blue-500 text-white">
\`\`\`

**تغيير الهوية البصرية بالكامل:**

فقط غيّر القيم في `:root` وسيتم تطبيق التغييرات على كامل الموقع تلقائيًا!

### 3. المكونات القابلة للاستبدال

**مكون العنصر النائب:** `components/VisualPlaceholder.tsx`

يُستخدم لجميع الصور والأيقونات والـ iframes:

\`\`\`tsx
<VisualPlaceholder 
  text="وصف العنصر"
  shape="rectangle" // أو circle أو square
  aspectRatio="16/9" // اختياري
/>
\`\`\`

**استبدال العناصر النائبة بمحتوى حقيقي:**

1. استبدل `<VisualPlaceholder />` بـ `<Image />` من Next.js
2. أو استبدله بـ `<iframe>` للمحتوى التفاعلي
3. احتفظ بنفس الـ className للحفاظ على التنسيق

## 📁 هيكل المشروع

\`\`\`
├── app/
│   ├── layout.tsx          # Layout رئيسي مع دعم RTL وMicrosoft Clarity
│   ├── page.tsx            # الصفحة الرئيسية (تجميع الأقسام)
│   └── globals.css         # الألوان المخصصة والأنماط العامة
├── components/
│   ├── HeroSection.tsx     # القسم العلوي مع popup
│   ├── PainPointsSection.tsx
│   ├── SolutionSection.tsx
│   ├── FinalCtaSection.tsx
│   ├── SmartFAQSection.tsx # أكورديون ذكي
│   ├── Footer.tsx
│   ├── SmartAmbassador.tsx # السفير الذكي (chatbot)
│   └── VisualPlaceholder.tsx
└── src/
    └── config/
        └── landingPageConfig.ts # ملف الإعدادات المركزي
\`\`\`

## 🎨 المكونات الرئيسية

### 1. SmartAmbassador (السفير الذكي)

زر عائم في أسفل يسار الشاشة يفتح نافذة محادثة:

**المميزات:**
- نبض خارجي ونقطة نابضة
- أصوات تفاعلية (Web Audio API)
- نافذة محادثة مع overlay ضبابي
- أزرار: إغلاق، كتم الصوت، مسح المحادثة
- محاكاة رد البوت بعد ثانية واحدة
- جاهز لدمج iframe من Typebot

### 2. HeroSection (القسم العلوي)

**المميزات:**
- دعم A/B Testing للعناوين
- شريط scarcity banner نابض
- popup لجمع البيانات (الاسم والواتساب)
- حالات: idle, loading, success, error
- التحقق من الحقول الفارغة

### 3. SmartFAQSection (الأسئلة الشائعة الذكي)

**السلوك:**
- شريط أفقي افتراضي مع تأثير نبض عند التحويم
- ينسدل عند النقر أو التحويم
- يختفي عند ابتعاد المؤشر
- أكورديون: إجابة واحدة مفتوحة في كل مرة
- دوران أيقونة السهم 180 درجة

## 🚀 التشغيل والتطوير

### التثبيت

\`\`\`bash
npm install
# أو
yarn install
\`\`\`

### التشغيل في وضع التطوير

\`\`\`bash
npm run dev
# أو
yarn dev
\`\`\`

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

### البناء للإنتاج

\`\`\`bash
npm run build
npm start
\`\`\`

## 🔧 التخصيص والتعديل

### تغيير المحتوى

**جميع** التعديلات على المحتوى تتم في `src/config/landingPageConfig.ts`:

\`\`\`typescript
// مثال: تغيير عنوان قسم نقاط الألم
painPoints: {
  title: 'العنوان الجديد هنا',
  points: [
    { text: 'نقطة ألم جديدة', solutionHint: 'الحل هنا' }
  ]
}
\`\`\`

### إضافة قسم جديد

1. أضف المحتوى في `landingPageConfig.ts`:

\`\`\`typescript
sections: {
  newSection: true, // أضف مؤشر boolean
}
newSection: {
  title: 'عنوان القسم الجديد',
  content: 'المحتوى هنا'
}
\`\`\`

2. أنشئ مكون جديد في `components/NewSection.tsx`:

\`\`\`tsx
import { config } from '@/src/config/landingPageConfig'

export function NewSection() {
  return (
    <section>
      <h2>{config.newSection.title}</h2>
      <p>{config.newSection.content}</p>
    </section>
  )
}
\`\`\`

3. أضفه في `app/page.tsx`:

\`\`\`tsx
{config.sections.newSection && <NewSection />}
\`\`\`

### تغيير الألوان

في `app/globals.css`، غيّر القيم في `:root`:

\`\`\`css
:root {
  --color-primary: #YOUR_COLOR;
  --color-secondary: #YOUR_COLOR;
  /* ... */
}
\`\`\`

### إضافة تأثيرات (Animations)

استخدم Tailwind CSS animations أو أضف animations مخصصة في `globals.css`:

\`\`\`css
@keyframes your-animation {
  /* ... */
}

.animate-your-animation {
  animation: your-animation 2s ease-in-out infinite;
}
\`\`\`

## 📊 Microsoft Clarity

تم دمج Microsoft Clarity في `app/layout.tsx` لتحليل سلوك المستخدمين:

**لتفعيله:**
1. سجّل في [Microsoft Clarity](https://clarity.microsoft.com/)
2. احصل على Project ID
3. استبدل `YOUR_PROJECT_ID` في `app/layout.tsx`

## ⚡ التحسينات التقنية

- ✅ Lazy loading للصور
- ✅ دعم RTL كامل
- ✅ Mobile-first responsive design
- ✅ SEO meta tags
- ✅ Web Audio API للأصوات
- ✅ Accessibility (ARIA labels)
- ✅ TypeScript للأمان
- ✅ Next.js 16 مع React 19

## 🎯 اختبار A/B

لاختبار متغيرات مختلفة للعنوان الرئيسي:

\`\`\`typescript
hero: {
  title: {
    variantA: 'العنوان الأول',
    variantB: 'العنوان الثاني',
    variantC: 'العنوان الثالث',
    active: 'variantB' // غيّر هنا للتبديل بين المتغيرات
  }
}
\`\`\`

## 🐛 استكشاف الأخطاء

### المحتوى لا يظهر؟
- تأكد من أن القسم مفعّل في `sections` في ملف الإعدادات
- تحقق من أن المكون مستورد في `app/page.tsx`

### الألوان لا تتغير؟
- تأكد من استخدام `var(--color-name)` وليس ألوان Tailwind المباشرة
- امسح الـ cache وأعد تشغيل الخادم

### الخطوط لا تظهر بشكل صحيح؟
- تأكد من أن `dir="rtl"` موجود في `<html>` في `layout.tsx`
- تحقق من تحميل خط Tajawal بشكل صحيح

## 📝 ملاحظات مهمة

1. **لا تعدّل المكونات مباشرة** - عدّل فقط في ملف الإعدادات
2. **استخدم الألوان المخصصة دائمًا** - لا تستخدم ألوان Tailwind المباشرة
3. **العناصر النائبة** - استبدلها بمحتوى حقيقي عند الجاهزية
4. **اختبر على الجوال أولاً** - التصميم mobile-first

## 📞 الدعم

للأسئلة أو المساعدة:
- البريد الإلكتروني: info@aiuncode.com
- تلغرام: [@tsahma](https://t.me/tsahma)

---

**بُني بـ ❤️ لـ AI-Uncode**
